const ifj = @import("ifj24.zig");

pub fn main() void {
    var board = ifj.string("----------------------------------------------------------------");

    var running = 1;
    while(running == 1) {
        ifj.write("Zadej tah:");
        ifj.readi32();
    }
}