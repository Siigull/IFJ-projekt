const ifj = @import("ifj24.zig");

pub fn sum(x : i32) i32 {
    var a : i32 = x + x;
    a = x + x;
    return a;
}

// Main Program
pub fn main() void {
    var i : i32 = 30;
    i = sum(i);
    ifj.write(i);
    return 12;
}